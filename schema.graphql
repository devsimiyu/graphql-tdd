schema {
    query: Query
    mutation: Mutation
}

type Post {
    id: ID!
    title: String!
    description: String!
}

type Query {
    listPosts: [Post]
    getPost(id: ID!): Post
}

type Mutation {
    createPost(title: String!, description: String!): Post!
}
